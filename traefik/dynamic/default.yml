http:
  middlewares:

    secureHeaders:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true

    gzip:
      compress: {}

    # Redirect www → non-www (tuỳ chọn, bỏ comment nếu muốn)
    # www-redirect:
    #   redirectRegex:
    #     regex: "^https://www\\.nextchat\\.vn(.*)"
    #     replacement: "https://nextchat.vn${1}"
    #     permanent: true
